{
  "name": "core",
  "version": "1.0.0",
  "description": "core business logic for project",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "babel src -d lib",
    "build": "babel src -d lib",
    "build:watch": "babel src -d lib --watch",
    "lint": "eslint src",
    "lint:fix": "eslint src --fix",
    "lint:watch": "esw src --watch --fix",
    "dev": "concurrently \"yarn build:watch\" \"yarn lint:watch\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@babel/cli": "7.4.4",
    "@babel/core": "7.4.5",
    "axios": "^0.18.0",
    "axios-cancel": "^0.2.2",
    "lodash-es": "^4.17.11"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.4.5",
    "@vue/eslint-config-airbnb": "^4.0.0",
    "concurrently": "^4.1.0",
    "eslint": "^5.16.0",
    "eslint-config-prettier": "^5.0.0",
    "eslint-plugin-prettier": "^3.1.0",
    "eslint-watch": "^5.1.2",
    "prettier": "^1.18.2"
  }
}
